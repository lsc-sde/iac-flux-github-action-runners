runner:
  scope: repo

  replicas:
    minimum: 0
    maximum: 5

  resources:
    limits:
      memory: 2Gi
    
    requests:
      cpu: 10m
      memory: 256Mi

accessToken:
  secretName: pat-token
  secretKey: token